# Core Autogen framework
pyautogen~=0.2.20

# For handling .env files to store API keys
python-dotenv

# For reading PDF source documents
pypdf

# For making a custom API call (if you were to use the tool-based approach)
# It's good practice to keep it even if not currently used by the primary method.
requests

# LiteLLM is the underlying library Autogen uses for all LLM API calls.
# While it's a sub-dependency of Autogen, explicitly listing it can sometimes
# help with dependency resolution and makes its role in the project clear.
litellm

functools